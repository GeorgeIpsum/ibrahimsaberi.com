import { AudioLines } from "lucide-react";
import type { ToolGroup } from "../types";

export const audioTools = {
  name: "audio",
  icon: <AudioLines />,
  size: "small",
  tools: [],
} satisfies ToolGroup;
