export default async function Page() {
	// const posts = await api.drops.allPosts.query();
	return (
		<div className="mx-auto w-full sm:w-150 md:w-172 lg:w-full">
			{/* <DropList drops={posts} /> */}
		</div>
	);
}
